class Solution:
    def heightChecker(self, heights: List[int]) -> int:
        d=sorted(heights)
        n=len(d)
        c=0
        for i in range(n):
            if d[i]!=heights[i]:
                c+=1
        return c
                
        
