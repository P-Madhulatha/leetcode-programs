class Solution:
    def canBeEqual(self, target: List[int], arr: List[int]) -> bool:
        arr.sort()
        target.sort()
        n=len(target)
        for i in range(n):
            if target[i]!=arr[i]:
                return False
        return True
        
        
